# SQL

DAY 1: SHOW,CREATE,DROP,USE,CREATE TABLE,INSERT INTO,SELECT * FROM.

DAY 2:  IF NOT  EXIT, IF EXIT , CREATING TABLE INSERTING DATA.

DAY 3 : CONSTRAINTS
1. UNIQUE
2. PRIMARY KEY
3. NOT NULL
4. FOREIGN KEY(PARENT TABLE,CHILD TABLE)
5. DEFAULT
6.CHECK:LIMIT THE VALUES
